# Communication Rule

- Coordinator-to-subagent work uses `brief` envelopes.
- Subagent outputs use `result`, `review`, `handoff`, `clarification_request`, or `error` envelopes.
- Experimental agent teams may use direct teammate messages and the shared task list.
- Resolve conflicts by comparing evidence, validation, and repository source-of-truth.
